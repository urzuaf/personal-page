---
import cocktails from '../data/cocktails.json'
import Layout from '../components/Layout.astro';

import cocktails_img from  "../assets/cocktails.png"
import {Image} from 'astro:assets';
---
<Layout title='animedoro'>
    <div class="py-10">
        <section class="flex flex-col items-center gap-4" >
            <a href={cocktails.url} target="_blank">
			    <Image quality={"mid"} src={cocktails_img} alt={cocktails.title} />
            </a>
            <h1 class="text-2xl md:text-5xl font-semibold" >{cocktails.title}</h1>
            <h4 class="text-zinc-500">Herramientas : {cocktails.Herramientas}</h4>
            <div class="px-8 flex flex-col gap-8 py-8 ">
                <h3 class="text-xl md:text-2xl font-medium text-center">Problema</h3> 
                <p>{cocktails.problem}</p>
                <h3 class="text-xl md:text-2xl font-medium text-center">Desarrollo</h3> 
                <p>{cocktails.Desarrollo}</p>
                <a class="btn  rounded p-4 text-center mb-4" href={cocktails.url} target="_blank" >Ver en vivo</a>
            </div>
        </section>
    </div>
</Layout>

<style>
    img{
        width: 100vw;
        padding-top:0 ;
        opacity: 0.8;
        margin-bottom: 4rem;
    }
    p{
        max-width: 55ch;
    }
    .btn{
		position: relative;
		border: 2px solid #2aea7d;
		padding: 16px 32px;
		transition: .3s ease;
	}
	.btn:hover{
		background: #2aea7d;
		transition: .3s ease;
	}
</style>